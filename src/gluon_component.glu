
#[implicit]
type AddComponent a = { component_name : String }
    
#[derive(Eq, Show)]
type Pos = { x : Float, y : Float }

let new_pos x y : Float -> Float -> Pos = { x, y }

#[derive(Eq, Show)]
type Vel = { x : Float, y : Float }

let pos_component : AddComponent Pos = { component_name = "Pos" }
let vel_component : AddComponent Vel = { component_name = "Vel" }

{
    Pos,
    Vel,

    AddComponent,

    eq_Pos,
    show_Pos,

    new_pos,
    pos_component,
    vel_component,
}
